# HANDOFF — noslab-elbrenz · 12 luglio 2026 (sera)

Documento di ripartenza. Sessione lunghissima e molto produttiva. **Tutto è committato e pushato su `origin/main` (ultimo commit `64a26f9`), working tree pulito.**

## Come ripartire (pre-flight)
```bash
cd ~/Sviluppo/noslab-elbrenz
gh auth switch -u associazioneelbrenz-ai && gh auth setup-git
git status                       # atteso: clean
git log origin/main --oneline -8 # dove siamo
git pull                         # se lavori da un'altra macchina
```
Deploy standard (sempre Claude): `npm run build` → `netlify deploy --prod --dir=dist --site=a8922ddb-53ec-4541-ac15-99570b61a1b2` → smoke test su elbrenz.eu. DNS resta a Cristian.

**Nota flag**: `GUARDIANI_LIVE=true` vive in `.env.local` (gitignored). Se cloni su una macchina nuova, riaggiungilo o i Guardiani tornano OFF al build.

---

## ✅ FATTO E LIVE oggi (12/7)
- **Bot Telegram Andreas** (@andreas_elbrenz_bot): comandi (/start /help /eventi /tessera /chatid) + domande RAG con fonti + resa markdown→HTML pulita. Ponte andreas-chat verificato.
- **Gita Giochi Medievali**: iscrizione + anticipo 30€ PayPal + bonus preorder −5€ (entro 31/7) + countdown 54 posti + landing + privacy dedicata.
- **Lead-gen**: download libro Altmayer (modulo nome+email, URL PDF non in chiaro) + popup soft "Resta in contatto" sul documentario (non blocca il video).
- **Sezione Editor COMPLETA e LIVE** (`/redazione`): backend (stati articolo, RLS, edge `articolo-azione` con sanitizzazione XSS, vista `v_articoli_pubblici`) + UI (login OTP → i miei articoli / crea-modifica bozza / invia; segretario → coda approvazione). **Everton Altmayer** (`blogtiroleses@gmail.com`) = primo editor (ruolo `collaboratore`). Provisioning socio automatico al login in `otp-verify`.
- **Rubrica "Una terra, cinque lingue"**: 20 uscite (2021-2026, solo El Brenz) → card a UT24.
- **Guardiani de la lenga**: LIVE **e riparati** (validazione via pagina `/guardiani-curatela` su elbrenz.eu; edge risponde JSON; Barbustel pubblicato). Punto 2 = "La Commissione Linguistica di El Brenz valida".
- **Fiöi dal Nos**: pagina d'onore (testo curato, tipografia long-form, video `JjYKr7hCRz0`, 1 cartina ALD recuperata, card in home).
- **Depurazione em-dash** integrale del sito.
- **Audit di sicurezza**: `docs/SECURITY_AUDIT_2026-07.md` (postura buona, nessun buco critico).

## ⚠️ GOTCHA IMPORTANTI imparati oggi (non ripetere)
1. **La piattaforma Supabase forza `text/plain` sull'HTML servito dalle edge function** → non renderizzare HTML dall'edge; servi la conferma da una pagina su elbrenz.eu che chiama l'edge in JSON. (Fix Guardiani.)
2. **Chiamare `andreas-chat` server-to-server: SOLO `apikey`, mai `Authorization: Bearer`** (la publishable key come Bearer attiva il ramo JWT → `invalid_jwt` e disattiva `isTrustedBot`).
3. **Webhook Telegram**: il `secret_token` del `setWebhook` deve combaciare con `TELEGRAM_WEBHOOK_SECRET`, altrimenti il bot rifiuta in silenzio (200 vuoto, pending 0).
4. **Provisioning utente**: `public.utente` ha `nome`/`cognome` NOT NULL; un insert con solo id+email fallisce. `utente_ruolo.utente_id` FK a `public.utente` (non auth.users).
5. **Niente `*_LIVE=true` senza smoke test E2E documentato.**

---

## 🔴 DA FARE — CRITICO
- **Fiöi dal Nos · 5 cartine mancanti**: copertina documentario, cartina etnica 1880, "Lingue romanze in Europa", tavola alfabeto retico, cartina-documento Dominici. **Sparite da WP** (404 post-cutover) e assenti dal Wayback. → servono gli **originali** da Cristian (Drive/disco). Solo la cartina Bauer/Goebl (ALD, CC BY-SA) è recuperata e in pagina.
- **Editor · test E2E**: Cristian accede a `elbrenz.eu/redazione` con la sua email e verifica la coda approvazione come admin.

## 🟡 ASPETTA MOSSA DI CRISTIAN (una alla volta, su suo OK)
- **Notifiche direttivo (fase 2)**: gruppo Telegram "Sala comando", `TELEGRAM_DIRETTIVO_CHAT_ID = -5532248837`. Il sistema notifiche è pronto (edge `telegram-bot` ramo notify + tabella `telegram_config`); va **registrato il gruppo** (`/attiva_notifiche` nel gruppo da admin) o salvato il chat_id in `telegram_config`, e confermati i 4 eventi (nuovo socio · iscrizione gita · convenzione · domanda sito).
- **Email Goebl**: GIÀ INVIATA oggi (id `4031a21c-...`). In attesa di risposta.
- **Mailing list**: 39 contatti da importare in Resend + email di ricontatto con opt-out (non fatto).
- **Lemmi-seed Guardiani** (5-8 termini) dalla Commissione.
- **Fix audit di sicurezza** (su OK): CSP enforced, Turnstile, GDPR (cookie/consensi/cancellazione), rotazione secret; e additivi sicuri (honeypot Sportello, restringere `ai_config_ruolo`, audit viste, X-Frame DENY).

## 🟢 ASPETTA INPUT ESTERNO
- **Carte ALD** in pagina + immagine card home → risposta di Goebl o URL da digitALD.
- **Gita fase 2** (saldo + reminder) → costo totale.
- **OCR libro Altmayer → KB Andreas** → testo pulito.
- **Mail Paternoster** (Union Ladin) → quando agli atti in `/crediti-e-licenze`, aggiungere Union Ladin al punto 2 Guardiani.

## 🔵 NON TOCCARE ORA (da progettare, non implementare)
PWA soci, Community, gestione APS/RUNTS, i18n DE/EN/PT. Mancano decisioni a monte. Se pensi servano preparativi → **chiedi prima**.

---

## Coordinate
- Repo: `associazioneelbrenz-ai/noslab-elbrenz` · Sito: https://elbrenz.eu
- Netlify site: `a8922ddb-53ec-4541-ac15-99570b61a1b2` · Supabase: `wacknihvdjxltiqvxtqr`
- Regole ferme: additività (mai rimuovere codice funzionante, `.bak` prima di modificare); niente em-dash nei testi pubblici; nessuna email a nome dell'Associazione senza OK di Cristian + prova a info@elbrenz.eu; privacy-first sugli embed (click-to-load); dopo ogni push `git log origin/main`.
