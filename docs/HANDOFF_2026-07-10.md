# HANDOFF — 10 luglio 2026
Documento di ripartenza. Sostituisce `HANDOFF_2026-07-08_cutover.md` come stato corrente. Progetto sito El Brenz · Astro 6 + Tailwind · repo `associazioneelbrenz-ai/noslab-elbrenz` · branch `main` · Supabase `wacknihvdjxltiqvxtqr` · Netlify `elbrenz-app` (site `a8922ddb-53ec-4541-ac15-99570b61a1b2`) · dominio primario apex `elbrenz.eu`.

## A. DECISIONI VINCOLANTI (9-10 luglio)
1. **Deploy = SEMPRE Claude, in automatico.** Ribadito da Cristian il 10/7 ("da adesso fai sempre tu il deploy… procedi sempre tu in automatico"). Dopo ogni modifica al sito: build → commit → push → `netlify deploy --prod`. NON lasciare il deploy a Cristian, NON chiedere. Il **DNS resta solo di Cristian**.
2. **PAGAMENTI_LIVE ratificato** (Step C). `src/lib/pagamenti.ts:14 = true`, costante build-time. Collaudato E2E live il 9/7 (pagamento reale → domanda collegata → approvazione → rimborso via webhook). Quota e donazioni entrambe operative. **Sistema tesseramento ufficialmente operativo.** L'annuncio pubblico lo fa Cristian con la chat web (Code non pubblica).
3. **TESSERE_LIVE = true** (secret Supabase, flippato il 9/7). Vedi stato invii al §C.
4. **PWA soci = LO STESSO Supabase del sito** (`wacknihvdjxltiqvxtqr`) — decisione Cristian 9/7. Ecosistema unico, un solo login OTP, zero sincronizzazioni. **Supera** la vecchia regola "PWA = nuovo Supabase". Restano separati: repo (nuovo), progetto Netlify, subdominio (proposta `app.elbrenz.eu`).

## B. FATTO E LIVE (9-10 luglio)
- **Fix link HMAC nel path** (non query string): i link email si corrompevano per l'encoding quoted-printable (`=`+2 cifre esadecimali → byte). Applicato a `convenzioni-proposta` E a `scheda-domanda` (+ i due caller `contact-form` e `paypal-webhook` che costruiscono il link, autorizzazione puntuale). Testato E2E su entrambi.
- **Feature Convenzioni Soci (M5.0 v2)**: tabella `convenzioni` (RLS deny-by-default) + vista pubblica `convenzioni_pubbliche` (mai il referente) + rate limit **persistente** `convenzioni_rate_limit` + `convenzioni_rl_hit` (IP hashato SHA256 — **deviazione motivata** dal brief: l'in-memory di contact-form non protegge su edge multi-istanza, 5/5 passate, cfr. AUD-B5). Edge function `convenzioni-proposta` (POST proposta → email Direttivo con 2 link HMAC Approva/Rifiuta → cortesia al referente; azione idempotente). Pagine `/convenzioni` (SSR) e `/convenzioni/schema-tipo` (fac-simile, da sostituire col testo KIT). Voce nav "Convenzioni" ON. Card "Convenzioni Soci" in home.
- **Filigrana Aquila Tirolensis** (pubblico dominio) nella home, fascia sotto la hero, opacità 0.05, nascosta su mobile. Credito nel footer ("Aquila Tirolensis di Matthias Burgklechner, XVII sec., pubblico dominio"). *(TODO: pagina /crediti-e-licenze dedicata quando si fa il kit legale.)*
- **Card Sostieni** redesign (foto rievocazione) · **widget Andreas M3.0** con bottone Diego attivo (+393471077636) · **bandiera ladina footer** ingrandita (48→72px) · **logo UT24** in home e nei crediti rubrica.
- **KB Andreas — Baratter, Storia del Trentino (Athesia 2017)**: sorgente unica, **30 chunk** (lotto 1: 1-14, lotto 2: 15-30), embedding OpenAI text-embedding-3-small. Nuova edge function `ingest-chunks` (contenuti pre-chunkati). **Stato: STAGING (`visibile_ospiti=false`)** in attesa dell'ok di Cristian sui chunk sensibili (9-10, 13-14, 17, 23, 29) → poi flip a true. Backfill editore/ISBN/note_interne già fatto. Test 7/7 domande verdi.

## C. TESSERE DIGITALI — stato e blocco
- `TESSERE_LIVE=true`. **Prova n.4** (Cristian) inviata a cristian.bresadola@gmail.com (col template vecchio).
- ⚠️ **Batch 18 IN ATTESA e da RIDISEGNARE**: Cristian vuole la tessera ridisegnata con **filigrana dorata Aquila** + **QR univoco** → pagina verifica real-time "socio in regola". Feature da specificare (bozza M5.5). NON inviare il batch col template vecchio.
- ⚠️ **Nodo tecnico**: non esiste percorso di re-invio tessera per soci GIÀ approvati (l'email parte solo alla transizione in_attesa→approvata). Invio fatto via script diretto `send_tessera.py` + send-email (anon key legacy). Per il batch serve il nuovo design + questo meccanismo.
- Casi email condivise decisi: n.13 Nadia Pangrazzi → diegomagnoni@live.it; n.14 M.L. Battistini → monica_valentinotti@hotmail.it con riga di avviso. Recapito Battistini DA ACQUISIRE. Nessun sollecito rinnovo prima del 31/12/2026.

## D. AUDIT M4.0 FASE 1
Report `docs/AUDIT_2026-07/AUDIT_REPORT_2026-07.md` + censimenti D1/D2/D3 + advisors + Lighthouse già prodotti. Findings ALTO: security header assenti (AUD-A1), privacy incompleta (AUD-D2: Anthropic/OpenAI/PayPal/Google Fonts non dichiarati), Google Fonts da self-hostare (AUD-E3). Nessun CRITICO sfruttabile. Fase 2 (fix) solo con ok di Cristian.

## E. CODA
- **UT24 import**: lista 30 candidati in `docs/UT24_backlog_candidati.md`, attende curatela di Cristian (esclusa sempre "Pillole di benessere").
- **Schema-tipo convenzioni**: sostituire fac-simile col testo definitivo (sez. B `KIT_convenzioni_soci_v1.md`).
- **Seed convenzioni**: NosLab e Punto Riflesso APS in 'proposta', beneficio da definire (attivare solo dopo delibera CD con astensione).
- **M6.0 PWA "Community del Brenz"**: brief pronto; schema community già in DB (forum_*, sala_*, messaggio, notifica, push_token, utente/ruolo). Parte dopo kickoff di Cristian.
- Post: fix audit Ondata 1 · kit legale (testi dalla chat web sui censimenti) · radar eventi F1 · tessera QR (M5.5).

## F. REGOLE VIGENTI
Additivo; `.bak`; diff minimi; zero nuove dipendenze npm; `astro check` 0 errori; account git `associazioneelbrenz-ai`; **deploy Netlify = Claude in automatico**, DNS solo Cristian; edge functions zona protetta salvo autorizzazione puntuale; `andreas-chat` INTOCCABILE; RLS verificata su ogni tabella toccata; secret solo nei dashboard/keychain (mai in chat); mai SQL distruttivo senza id/filtri espliciti + ok; email di test col nominativo "TEST TECNICO"; contenuti in linea editoriale (Tirolo storico ≠ Tirol, mai "dialetto", motto ladino mai tradotto nel markup).

## G. AGGIORNAMENTO 10/7 pomeriggio — Tessere QR (M5.5) COMPLETE + Wallet
- **Batch 19/19 inviato** (n.4 prova + 18 batch, tutte OK; n.13→email Diego, n.14→email Monica con riga avviso). `tessera_inviata=true` e `codice_tessera` per tutti. Test "TEST TECNICO TEMPLATE" (n.20) eseguito e RIPULITO: numerazione ripristinata (max=19, il prossimo socio reale prende la n.20).
- **Template scheda-domanda allineato** al design QR (autorizzazione puntuale 10/7): rendering condiviso in `_shared/tessera.ts`, logica di approvazione invariata, `.bak` in repo.
- **Wallet FASE A live**: pagina /tessera/{codice} con bottoni "Scarica la tessera" → edge fn `tessera-download` (PNG verticale 1080×1686 + PDF A6; resvg-wasm + pdf-lib + font Playfair BUNDLATI — `supabase/config.toml` creato per gli static_files; cache su Storage `tessere/wallet/v1/`). Sezione "Portala sempre con te" (Aggiungi a Home iOS/Android). Email tessera: riga "Scarica la versione per il telefono" per i FUTURI invii (i 19 già inviati non si reinviano: la pagina è già nel loro QR).
- **Wallet FASE B (Google) PREDISPOSTA, NON LIVE**: edge fn `wallet-google` deployata ma dormiente (503 finché `WALLET_GOOGLE_LIVE` != 'true'); bottone in pagina nascosto dietro lo stesso flag (env Netlify). Passi di Cristian per attivarla:
  1. https://pay.google.com/business/console con associazione.elbrenz@gmail.com → richiedi accesso **Google Wallet API** → annota **Issuer ID**;
  2. Google Cloud Console → progetto (nuovo o esistente) → abilita "Google Wallet API" → crea **Service Account** → genera chiave JSON;
  3. Wallet Console → Utenti → aggiungi l'email della service account con permessi di gestione;
  4. Supabase secrets: `GOOGLE_WALLET_ISSUER_ID`, `GOOGLE_WALLET_SA_EMAIL`, `GOOGLE_WALLET_SA_KEY` (campo private_key del JSON, PEM completo);
  5. flip `WALLET_GOOGLE_LIVE=true` nei Supabase secrets E nelle env Netlify → il bottone appare.
- **Wallet FASE C (Apple) NON implementata — decisione in sospeso Cristian/CD**: richiede iscrizione **Apple Developer Program (99 $/anno)** per il certificato **Pass Type ID** con cui firmare i .pkpass. Senza certificato non esiste via legittima. Da valutare se il costo vale il beneficio rispetto a Fase A (pagina Home screen) + Google Wallet.

## H. CHIUSURA SESSIONE 10/7 (pomeriggio) — Ondata 1 + Convenzioni attive + OG
### Tessere / Wallet
- **Fase A CHIUSA** (verificata da Cristian sul vivo, nessun ritocco richiesto). Stato: **19/19 tessere inviate**, prossimo numero **20** (sequenza verificata: max=19, seed=20). Nessun reinvio ai 18 del batch: la riga "Scarica la versione per il telefono" è entrata nel template DOPO il batch (vale per i futuri invii).
- **Fase B (Google Wallet)**: in attesa del setup Console di Cristian (issuer, service account, secrets `GOOGLE_WALLET_*`); `WALLET_GOOGLE_LIVE=false`, funzione dormiente. NON toccare fino al suo avviso.
- **Fase C (Apple Wallet)**: in sospeso Cristian/CD — richiede Apple Developer Program (99 $/anno, certificato Pass Type ID).
- **PENDENTE (gate)**: email ai 19 soci con bottone **integrazione 10 €** (quota 10→20 €). Serve: ok sul meccanismo (tipo 'integrazione' hardcoded 10.00 server in paypal-create-order — autorizzazione puntuale), ok sul testo, prova a Cristian, batch.

### Audit M4.0 — Ondata 1 ESEGUITA (deploy Netlify finale a cura di Cristian)
| Finding | Commit | Esito |
|---|---|---|
| AUD-A1 security headers + CSP Report-Only | `fix(security): security headers…` | public/_headers: HSTS 1y, nosniff, XFO, Referrer-Policy, Permissions-Policy; CSP Report-Only da osservare e poi irrigidire |
| AUD-F2a 404 brandizzata | `fix(seo): pagina 404…` | src/pages/404.astro |
| AUD-E3 font self-hosted | 2 commit `fix(privacy…)` | 7 woff2 variabili in /fonts/ (Playfair, Inter, Source Serif 4, Cormorant); zero chiamate a Google. Bonus: l'@import di andreas-chat.css era in posizione invalida → i browser lo ignoravano, ora Cormorant è servita davvero. Residuo innocuo: condizione morta in sw.js |
| AUD-C3 npm audit fix | `fix(deps)…` | 27→16 vulnerabilità, 3 high→0 (astro 6.4.8). Residue 16 low/moderate: servono @astrojs/netlify@8 (breaking) → upgrade da pianificare |
| AUD-B3 CORS whitelist | `fix(security): CORS…` | otp-request, otp-verify, andreas-hofer: whitelist dinamica (stessa di contact-form) + Vary: Origin; send-email statica apex. Deployate e verificate |
| AUD-C2a cap input Andreas | `fix(security): cap input 600…` | 600 caratteri (deroga puntuale), server 400 query_too_long + maxlength 600 e contatore live nel widget. Deployata e verificata |

**Smoke test post-deploy (fare col deploy di Cristian)**: form tesseramento + chat Andreas dalla pagina e dal widget (CORS + cap), header su elbrenz.eu (`curl -sI`), font senza richieste a Google (DevTools), OG con https://www.opengraph.xyz o condivisione WhatsApp.

### Convenzioni M5.0 — ATTIVE (delibera CD 2/2026 del 10/07, unanimità, astensione Segretario)
- **NosLab S.a.s.** e **Punto Riflesso APS** → stato 'attiva' a DB con benefici/dettagli deliberati; 8 loghi (PNG+WebP intatti) su Storage `assets-pubblici/convenzioni/loghi/`.
- **/convenzioni**: card ridisegnate (pannello avorio bordo oro, area logo altezza fissa, tag categoria, fallback iniziale su cerchio oro).
- **Home**: card Convenzioni ridisegnata (portale con tag, scelta Cristian al gate) + **marquee "Con noi nelle valli"** (CSS-only, hover:hover per il grayscale — touch a colori, reduced-motion statico, loghi da vista pubblica a build time).
- **Schema-tipo**: testo DEFINITIVO dal KIT sez. B (Step 3.1 PROMPT_SOSPESI chiuso). Step 3 COMPLETO.

### SEO
- **OG image per ogni pagina** (27, brandizzate, stile tessera) + prop ogImage nei wrapper PaginaPilastro/PaginaIstituzionale. AUD-F2b sostanzialmente chiuso.

### Ancora attesi da Cristian
- **Vecchio hosting WordPress: NON toccato** — propagazione DNS in coda, ricontrollo **lunedì 13/7** (voce aggiunta con la verifica generale, D2).
- Lista UT24 spuntata → Step 6 PROMPT_SOSPESI (import primo lotto).
- Setup Google Wallet Console → flip Fase B.
- Ok su meccanismo+testo integrazione 10 € → invii.
- Deploy Netlify finale ondata (dist/ già buildata e verificata) + smoke test insieme.
- KB Andreas: prossime fonti per l'addestramento (pipeline ingest-chunks pronta).

## I. CONSOLIDAMENTO FINALE 10/7 sera — produzione == main (verificato)
### Esito reale post-deploy (curl sul vivo, non a memoria)
- **Home**: card Convenzioni nuova (pannello avorio, bordo oro, **SVG portale con tag** — NON stretta di mano: scartata al gate dopo 3 iterazioni non pulite, portale scelto da Cristian dall'artifact di confronto e poi dettagliato su sua richiesta: conci, chiave di volta, imposte, corsi di pietra) + marquee "Con noi nelle valli" attivo (16 loghi = 2 attive × 8 ripetizioni, lette a build time). Regole `@media(hover:hover)` + `grayscale(40%)` + `prefers-reduced-motion` verificate nel bundle CSS live (Layout.*.css).
- **OG**: og-home/convenzioni/storia/tesseramento serviti (200, ~93-95KB), meta og:image corretti.
- **404** personalizzata live · **font**: zero riferimenti a Google nel HTML, 4 famiglie woff2 servite da /fonts/ (200).
- **/convenzioni** integra (2 attive con loghi) · **/dona** 200 — toccata SOLO per aggiungere `ogImage="/og/og-dona.jpg"` (task OG per pagina, nessun'altra modifica) · form tesseramento carica · **andreas-chat risponde** (RAG onesto su domanda di prova).
- **Integrazione 10 €**: meccanismo in produzione (create-order tipo 'integrazione' 10.00 server-side, pagina /integrazione/{codice}, edge integrazione-invio) ma **CONGELATA: zero email inviate, nemmeno la prova** — attesa ok di Cristian su testo e prova.
- **Invariati**: Google Wallet flag false (attesa setup Console Cristian) · lista UT24 attesa · Ondate 2-3 audit in attesa.

### ⚠ LEZIONE DI PROCESSO (da Cristian, vincolante)
**Mai dichiarare un blocco "fatto e pushato" con edit o agent pendenti.** D'ora in poi ogni report di chiusura blocco include l'output di `git status`. (Caso concreto: le modifiche integrazione erano deployate sulle edge ma non ancora committate al momento del report — la produzione deve sempre corrispondere a main.)

## L. AGGIUNTE SERALI 10/7
- **Upload logo self-service** nel form /convenzioni: file facoltativo (PNG/JPG/WebP ≤1MB, magic bytes server-side) → bucket PRIVATO `convenzioni-staging` → pubblicato su `assets-pubblici/convenzioni/loghi/proposta-{id}.{ext}` SOLO all'approvazione HMAC (colonna additiva `logo_staging_path`). Testato E2E live e ripulito.
- **Punto Riflesso non è più APS** (ora marchio NosLab indipendente): `nome_attivita` corretto a DB in 'Punto Riflesso' (10/7, indicazione Cristian). Nota: i verbali/delibere citano la denominazione dell'epoca.

## M. VERIFICA GENERALE 10/7 (sola lettura) — esiti e chiusure
- Report: main==produzione (diff solo hash `_astro`), DB/Storage/functions conformi, 7 difformità (D1-D7).
- **D1 CHIUSA**: migration `20260710123132` a registro col DDL del 10/7 + `migration repair` per i 4 file di luglio: ogni migration in repo è ora registrata a DB. **RESTA il debito storico**: ~30 voci aprile-maggio nel registro remoto SENZA file locali → consolidare con baseline `db pull` in sessione dedicata.
- **D2 CHIUSA**: voce WordPress/DNS (ricontrollo lunedì 13/7) aggiunta agli attesi.
- **D4 CHIUSA**: .bak sparsi spostati in `.backups/` (non tracciati, nessun commit).
- **D3/D6/D7 chiuse così** (cache wallet on-demand · 19+4 email test autorizzate · card home = PORTALE per decisione al gate).
- **D5 — NOTA VINCOLANTE PER IL FUTURO CODE**: il secret `WALLET_GOOGLE_LIVE` **NON esiste ed è GIUSTO così** — l'assenza equivale a `false` (verificato: wallet-google risponde 503). Lo creerà **Cristian** `=true` all'attivazione, insieme ai `GOOGLE_WALLET_*`. **Non "riparare" il secret mancante di propria iniziativa.**
- **FIX marquee mobile** (post-verifica, segnalazione iPhone/Safari): striscia vuota a tratti — causa lazy-load + collasso larghezza img senza dimensioni; fix eager + width/height letti a build time + ripetizioni/durata calcolate. Commit dedicato. **Dist ribuildata e verificata: PRONTA, deploy a cura di Cristian.**

## N. INTEGRAZIONE 10 € — INVIATA (10/7 sera, ok di Cristian su testo e prova)
- Prova n.4 approvata → **batch 18/18 OK** (19/19 totali). Recapiti condivisi come per le tessere: n.13→diegomagnoni@live.it, n.14→monica_valentinotti@hotmail.it con riga di avviso. Esiti in scratchpad di sessione, riepilogo nel report a Cristian.
- Ogni email porta a `/integrazione/{codice}` (importo 10,00 € blindato server-side, tipo 'integrazione', domanda_id agganciata alla creazione → riconciliazione automatica nel webhook con etichetta dedicata nella notifica al Direttivo).
- Monitoraggio pagamenti: `SELECT ... FROM pagamenti_tesseramento WHERE tipo='integrazione'` — al 10/7 sera 0 pagamenti (le email sono appena partite). Nessun sollecito automatico previsto: eventuali promemoria li decide Cristian.

## O. CHIUSURA SESSIONE 10/7 notte — collaudo integrazione E2E ✓
- **BUG al primo pagamento reale**: il CHECK `pagamenti_tesseramento_tipo_check` ammetteva solo quota/donazione → INSERT tipo='integrazione' violava il vincolo → 500 prima di ogni riga (zero 'creato', zero ordini PayPal), banner errore in pagina. **FIX**: CHECK allargato ai tre tipi (migration `20260710152523` a registro, disciplina D1). Lezione: quando si aggiunge un valore a un campo enum-like, verificare i CHECK constraint E testare un INSERT reale, non solo il ramo di validazione.
- **Collaudo E2E con denaro vero (Cristian, tessera n.4)**: pagina /integrazione/{codice} → PayPal → riga `completato`, 10,00 € (importo mai dal client), `domanda_id` agganciata, order+capture registrati, notifica "Integrazione quota 2026 (10 €)" a info@. **Flusso operativo per i 18 soci restanti**: riconciliazione automatica, monitorare con `tipo='integrazione'`.
- Nota: il primo "impossibile raggiungere il sito" era un blip DNS/rete del dispositivo (URL 200 stabile, DNS coerente sui resolver pubblici, link nel path corretto) — nessun intervento.

### Stato a fine giornata 10/7
- FATTO e live: tessere QR 19/19 · wallet Fase A · audit Ondata 1 (6 finding) · convenzioni attive con loghi + upload self-service + marquee (fix iOS) · schema-tipo KIT · OG 27 pagine · integrazione 10€ inviata 19/19 e collaudata · verifica generale D1-D7 chiuse.
- IN ATTESA di Cristian: setup Google Wallet Console (flag assente = false, NON ripararlo) · lista UT24 → Step 6 · ricontrollo DNS WordPress lunedì 13/7 · Fase 2 audit (Ondate 2-3) · baseline migration db pull (debito storico aprile-maggio) · pagamenti integrazione degli altri 18 in arrivo.

## P. MAPPA DELLE VALLI (M-F1) — Fase 1 COMPLETA (10-11/7), Fase 2 approvata
- **STEP 0 verificato sul campo**: WMS PAT CTP2020+Ortofoto2015 servono EPSG:3857 nativo (no proj4), tile medie 0,3-0,4s, licenza CC BY, WMTS PAT rotto (GWC out of synch) → WMS puro. Fallback OSM.
- **Dati**: `luoghi_interesse` (RLS deny, CHECK collaudati con insert reali), viste `v_luoghi_mappa` (LA PWA SOCI LA EREDITERÀ TAL QUALE) e `v_convenzioni_mappa`; convenzioni: lat/lng/mostra_in_mappa (le 2 attive appaiono solo quando Cristian le geolocalizza). Seed 3 luoghi in BOZZA (Castel Thun, Mulino Ruatti, Forte Strino — coordinate Nominatim): li pubblica Cristian.
- **/mappa**: SSR (punti freschi a ogni richiesta, senza rebuild), Leaflet 1.9.4 npm import dinamico SOLO al click (TBT+privacy), marker brand, filtri tipo/valle in querystring, elenco testuale server-rendered (a11y+SEO), switcher CTP/Ortofoto, attribuzione CC BY. Cuciture: nav Temi, link da /convenzioni, NUOVA /crediti-e-licenze, paragrafo in cookie policy.
- **Fase 2 (catasto asburgico 1861) APPROVATA strada C**: mappe d'impianto PAT GIÀ GEOREFERENZIATE (JPG+JGW EPSG:25832, 0,32m/px, CC BY) da idt.provincia.tn.it — 478 comuni, ~97 nelle valli (dist. Cles+Fondo+Malé). Sequenza: Fase 1 live e verificata → pilota Malé (im151.zip) con gate su resa/peso/allineamento (chiesa, ponte, incrocio) → batch 97 comuni tile z12-16 (NIENTE z17), bucket dedicato `catasto-storico`, cache immutable. Attribuzione approvata: "Elaborazione El Brenz su mappe d'impianto del catasto asburgico (1861), © Provincia Autonoma di Trento — Servizio Catasto, licenza CC BY".
- ⚠️ **Nota georeferenziazione (da Cristian)**: i JGW sono la georeferenziazione "di massa" della Provincia — perfetta per un overlay a opacità, ma se su qualche foglio il 1861 "scivola" rispetto al presente NON è un nostro bug. Saperlo prima che lo segnali un socio.
- 📈 **Monitoraggio egress (da Cristian)**: le tile del catasto usciranno dal bucket Supabase — a traffico normale il free tier regge; se la mappa diventa popolare, piano B = spostare le tile su Cloudflare R2 (egress gratuito). SOLO monitoraggio, nessuna azione ora.
- 🎁 Bonus da STEP 0 Fase 2: il SIAT espone WMS ortofoto 1973 e 1994 (`ofdtn73`/`ofdtn94`) — possibile futuro layer "com'era 50 anni fa" a costo zero.

## Q. SESSIONE 11/7 — Sportello, UT24, footer pro bono
- **SPORTELLO EL BRENZ «Porta la tua Storia» COMPLETO su /contatti** (card esistenti intatte): bivio Chiedi/Porta, 10 categorie con campi condizionali, upload staging privato `contatti-staging` (magic bytes, 3×5MB), codice pratica atomico EB-YYYY-NNN, doppia email (notifica [SPORTELLO] + auto-reply con codice; frase restituzione originali per il prestito), rate limit persistente 5/h. **Testato E2E su TUTTE le categorie** + negativi + rate limit; cleanup totale, progressivo azzerato. ⚠️ Il punto 5 del brief è arrivato TRONCATO ("test di OGNI categoria end-to-end (submit vero +"): interpretato come submit vero+cleanup+report — se c'era altro, dirlo a Code.
  - **Fuori scope → fase 2** (voci da brief): registrazione audio memorie orali (MediaRecorder) · attestato di donazione PDF automatico · pannello gestione pratiche (andrà nella PWA).
- **UT24 FASE A COMPLETA**: inventario in `docs/UT24_inventario_completo.md` — **401 uscite** (rubrica SETTIMANALE dal 3/2/2018, non mensile dal 2020), **zero mesi buchi**. FASE B FERMA: gate Cristian sull'elenco + conferma scritta UT24. Nota: 401 articoli = l'import in batch da 10 sarà lungo (~40 batch); da valutare se partire dai più recenti.
- **Footer**: dicitura pro bono NosLab + riga in /crediti-e-licenze.
- **Mappa Fase 1**: COMPLETA e committata (vedi §P) — dist buildata, ATTENDE deploy di Cristian e verifica insieme; poi pilota Malé (gate) per la Fase 2.

## R. VETROFANIA — predisposizione web (11/7)
- **⚠️ REGOLA FERREA URL: `/socio` = URL PERMANENTE PER STAMPA** (vetrofanie, adesivi, locandine). Mai rimuovere, mai rinominare: se la destinazione cambia si aggiorna il target del redirect in `public/_redirects` (oggi → /tesseramento, 301, querystring inoltrata — verificato su draft deploy: utm sopravvivono intatti).
- Tracciamento sorgente: `domande_tesseramento.sorgente_utm` jsonb (utm_source/medium/campaign letti dalla querystring SOLO alla submit; zero cookie). Query per il futuro: `SELECT count(*) FROM domande_tesseramento WHERE sorgente_utm->>'source'='vetrofania'`.
- Il QR per la vetrofania lo genera Cristian in chat puntando a `https://elbrenz.eu/socio?utm_source=vetrofania&utm_medium=qr&utm_campaign=convenzionati`.
- **FUORI SCOPE (annotato, non fare)**: Satispay come metodo di pagamento su /tesseramento (cantiere futuro: API Satispay Business, stesso pattern riconciliazione PayPal) · QR dinamici lato server.

## S. SESSIONE 11/7 pomeriggio — Widget, Sportello-bis, card home
- **Widget Andreas ridisegnato** (deroga SOLO layout): desktop 420px/75vh colonna flex (messaggi scrollabili, input fisso), mobile full-screen sheet 100dvh+safe-area (fix tastiera iOS), scroll-lock, focus trap, blocco WhatsApp collassabile dopo il 1° messaggio, X 44px. ⚠ Collaudo visivo multi-viewport NON eseguito (estensione Chrome non connessa): serve ritest di Cristian dal telefono.
- **Sportello-bis**: hero fotografico (4 file di Cristian INTATTI su assets-pubblici/sportello/), ancora #porta (apre il percorso offerta), radio Custodi obbligatorio (client+server) nelle 4 categorie archivio, tabella+vista custodi_memoria (curatela MANUALE), pagina SSR /custodi-della-memoria, voce footer, credito immagine AI in /crediti-e-licenze, CC Diego per tempo_competenze PREDISPOSTA e SPENTA (secret SPORTELLO_CC_TEMPO_COMPETENZE assente).
- **Card home 'Porta la tua Storia'**: tra articoli ed eventi (scelta Cristian al gate), SVG scatola+foto (2 iterazioni ok).
- **Mappa**: LIVE ma vuota per design — seed in bozza in attesa di revisione+pubblicazione di Cristian; convenzioni senza coordinate. Al suo 'pubblica': UPDATE e i marker appaiono (SSR, zero rebuild).
- Brief Sportello punto 5 e Custodi punto 3 arrivati TRONCATI nel canale: interpretati (submit vero+cleanup+report; elenco per anno con anonimi aggregati) — verificare se mancava altro.

## T. SESSIONE 11-12/7 — tesseramento avanzato, mappa home, fix email, blocco cartine
- **Massimo Paternoster** approvato via flusso ufficiale: socio **n.20**, tessera inviata (unionladinnonesa@gmail.com). Prossimo socio reale = n.21.
- **TESS 2** bottoni ✓Approva/✗Rifiuta in email notifica: token nel path 7gg monouso, pagina di CONFERMA con bottone (anti-anteprima), rifiuto con motivo interno, esito con link tessera. E2E ok, cleanup verificato.
- **TESS 3** email di grazie a integrazione completata: `_shared/integrazione.ts`, nel webhook (idempotente, link tessera esistente, avviso n.13/14). Collaudo retroattivo inviato SOLO a n.4 (Cristian) l'11/7 22:26 — attesa conferma resa.
- **TESS 4** solleciti integrazione: edge `solleciti-integrazione` DORMIENTE dietro `SOLLECITI_LIVE=false`; +10/+24gg, max 2, controllo pagamento live, registro idempotente `solleciti_integrazione`, colonna `integrazione_richiesta_il` (backfill 19 storici al 10/7). Dry-run verificato (0 invii, n.4 escluso, primo sollecito 20/7). **Cron NON attivato**: lo accende Cristian col flag (SQL nel file migration). Testo sollecito approvato.
- **FIX email tessera** (dark-mode iOS): card avorio, `color-scheme:light`, testi verde foresta, bandiera con bordo, box QR con bordo, filigrana intatta. Reinvio reale n.4 l'11/7 22:52.
- **Home**: banda "La Mappa delle Valli" (ortofoto PAT reale + pin), card Porta/Convenzioni fotografiche, sigillo portale su /convenzioni, micro-link Custodi, em-dash bonificati (regola in memoria).
- **⛔ BLOCCO TASK 1 (rescue cartine WP)**: `www.elbrenz.eu/wp-content/...` NON serve più il WP (dopo cutover → Netlify 404): download diretto impossibile. Wayback recupera solo **37/153** (116 persi). L'`Atlas-Tirolensis.png` flaggato nel brief è uno **screenshot del portale tiris © Land Tirol**, NON la carta PD 1774: non ri-ospitabile. **Serve da Cristian: l'origine Aruba del vecchio WP** (unico modo per recupero completo a piena qualità), e la sostituzione dell'Atlas con la vera incisione PD (Wikimedia/Rumsey). PAT e Gemini: come da brief, segnalati non toccati.
- **Guardiani de la lenga (TASK 2)**: TASK 0 ricognizione fatta — `dizionario_lemma` e `archivio_audio` ESISTONO a 0 righe, ricche; nessuna pagina Guardiani. Costruzione in coda al gate cartine.
