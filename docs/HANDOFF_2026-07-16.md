# HANDOFF — 2026-07-16 (sessione Code)

Base: `035f053`. Fine sessione: `0faa22a` su `origin/main` (+ commit docs).
Account: GitHub `associazioneelbrenz-ai` · Supabase `wacknihvdjxltiqvxtqr`.

---

## ✅ TASK — Sistema notifiche direttivo Telegram, config-driven (FATTO, live e verificato)

Chiude il pendente del 14/7 ("Notifiche direttivo Telegram — conferma eventi"). Passa da notifiche inline sparse a **un modulo unico config-driven** che copre i buchi dell'ecosistema, **senza toccare** le notifiche già funzionanti. Commit `72bce28`.

### Post-mortem: le notifiche live erano 6, non 4
Il brief contava 4 notifiche inline. Nel repo ce n'erano **6**, tutte via lo stesso hop HTTP all'edge `telegram-bot` (header `X-Bot-Secret: BOT_ANDREAS_SECRET`, body `{notify, text}` in markdown; `telegram-bot` risolve il `chat_id` da `telegram_config` e fa escape+HTML):
1. `scheda-domanda` → nuovo socio
2. `gita-cattura-ordine` → gita
3. `convenzioni-proposta` → convenzione
4. `contatti-submit` → contatto **E** Sportello/«Porta la tua Storia» (stesso edge, una notifica con etichetta)
5. `download-lead` → lead
6. `articolo-azione` → redazione ("Articolo da approvare"/"pubblicato")

Conseguenza: `lead`, `redazione` e `custode_sportello` **non erano buchi** → non riagganciati, per non generare doppioni.

### Architettura (decisa con Cristian)
- **Tabella toggle** `telegram_notifica` (`tipo` PK, `categoria`, `etichetta`, `attivo`, `updated_at`). RLS on, **nessuna policy** → solo service_role (edge). Migration `supabase/migrations/20260716111044_telegram_notifica.sql`, applicata al DB remoto **via MCP** (vedi nota migration history sotto).
- **Modulo condiviso** `supabase/functions/_shared/notificaDirettivo.ts`: `notificaDirettivo(supabase, tipo, dati)`. Legge il toggle (assente o `attivo=false` → esce in silenzio), compone testo a **PII minima** (nome/importo/lemma + link vista admin, mai email/telefono), invia **RIUSANDO l'hop `telegram-bot`** (una sola meccanica Telegram nel repo, niente `sendMessage` diretto). **Best-effort totale**: try/catch, non lancia mai, non blocca il flusso chiamante.

### Toggle attivi (tutti ON)
`pagamento_quota` · `donazione` · `integrazione_quota` · `ricevuta_bonifico` (Pagamenti) · `guardiani_lemma` (Guardiani) · `alert_anomalia` (Alert). Inseriti **solo** i tipi realmente inviati (niente toggle inerti).

### Eventi agganciati (i buchi veri)
- **`paypal-webhook`** (funzione `notificaCompletato`, dentro il guard idempotente `notificato` → una sola volta per pagamento): `pagamento_quota` / `donazione` / `integrazione_quota` a seconda di `riga.tipo`; **`alert_anomalia`** se una quota resta orfana (né agganciata né creabile).
- **`ricevuta-ocr`**: `ricevuta_bonifico` dopo l'insert del bonifico (`metodo='bonifico'`, `stato='in_verifica'`; flag `anomalia` in coda al messaggio).
- **`guardiani-contributo`**: `guardiani_lemma` dopo l'insert del lemma (`stato='in_revisione'`). Sorgente **edge**, niente trigger.

Nota anti-doppioni: `paypal-capture-order` **non** notifica (verificato) → il webhook è l'unico punto pagamento.

### Deploy + verifica
- Redeploy `--no-verify-jwt`: `paypal-webhook`, `ricevuta-ocr`, `guardiani-contributo` (il modulo `_shared` è bundlato al deploy; il deploy pulito conferma che compila).
- **Test E2E reale**: lemma di prova via `guardiani-contributo` (HTTP 200, edge v12) → **notifica confermata arrivata nel gruppo direttivo** con formato e PII corretti. Riga di test (`dizionario_lemma` + `guardiani_contributori`) **ripulita** (0 residui). Due email di test finite su **info@elbrenz.eu** (ignorabili).

### Controllo anti-spam (Cristian, senza deploy)
```sql
update telegram_notifica set attivo=false where tipo='ricevuta_bonifico';
```

### Additività
Le 6 notifiche pre-esistenti **non toccate** (restano inline, non passano dal toggle).

---

## ✅ TASK — download-lead: PII minima nella notifica (FATTO, live)

Su richiesta di Cristian (regola §8 PII minima). Commit `08c1a07`. Modifica **minima** al solo testo della notifica Telegram:
- Prima: `📖 Nuovo download del libro` + `Mario Rossi · mario@example.com`
- Ora: `📖 Nuovo download del libro` + `M.R. · A proposito di Tirolo fino al Lago di Garda`

Iniziali dal nome + titolo risorsa; flag consenso newsletter mantenuto. Dati completi (nome/email/telefono) restano in `download_lead` dietro autenticazione. Redeploy `--no-verify-jwt`.

---

## ✅ TASK — Home: immagini sezioni Guardiani e Atlante + richiamo laterale (FATTO, deployato prod)

- **Immagini home** (commit `e8846f6`): le card «Guardiani de la lenga» e «Le valli nell'Atlante del Ladino» passano dal segnaposto SVG alla **foto teaser reale** (WebP 696×383 self-hosted in `public/img/home/`), come le altre sezioni con immagine (Mappa, Libro). Testi e CTA invariati. `width`/`height` espliciti → niente layout shift. Le immagini erano in `~/Downloads` (scaricate da Cristian), copiate nel repo.
- **Richiamo laterale "Diventa socio"** (commit `5d6e224`): badge oro col logo El Brenz, fisso a sinistra, pulsa e si espande all'hover verso `/tesseramento`. Nascosto su mobile, reduced-motion safe. (Era una PROVA non committata; spedita su OK "deploy di tutto".)
- **Deploy prod** Netlify fatto; smoke live su elbrenz.eu OK (home 200, immagini `image/webp` 200, CTA intatti).

---

## ✅ TASK — Cruscotto direttivo Telegram · Fase 1 (FATTO, deployato · test lato Cristian)

Primi comandi di gestione del bot per il direttivo, SOPRA il linking + risoluzione ruolo già in `telegram-bot`. Solo edge-code, **nessuna migration**. Commit `0faa22a`. Additivo: comandi e flussi esistenti invariati.

- **Helper** (vicino a `sendMessage`): `escHtml`, `risolviRuolo` (telegram_link → utente_ruolo → livello max, stessa logica del ramo testo libero), `gateAdmin` (DM-only + livello≥50, rifiuto generico).
- **`/cruscotto`** — DM-only, admin. Conteggi read-only. **Stati REALI verificati nel DB**: soci = `domande_tesseramento.stato='approvata'`; domande in sospeso = `'in_attesa'`; lemmi da validare = `dizionario_lemma.stato='in_revisione'`; convenzioni proposte = `convenzioni.stato='proposta'`; lead ultimi 7 giorni = `download_lead.created_at >= now()-7d`.
- **`/guardiani`** — DM-only, admin. Lemmi `in_revisione` (max 10 + "…e altri N"), **nome** del contributore via join `contributore_id → guardiani_contributori.nome` (mai email; `dizionario_lemma` non ha `contributore_nome`), link a `/guardiani-curatela`.
- **`/notifiche [on|off <tipo>]`** — DM-only, admin. Elenca e commuta i toggle di `telegram_notifica` da Telegram.
- **Sicurezza**: gate = ruolo El Brenz dal linking (NON admin-di-gruppo, che resta solo per `/attiva_notifiche`). Comandi non elencati nel `/help` pubblico. PII minima, query strutturate, mai RAG. Deploy `--no-verify-jwt` fatto.

**Snapshot cruscotto ora**: Soci 20 · Domande sospese 4 · Lemmi da validare 1 · Convenzioni proposte 0 · Lead 7gg 6.

**⚠️ AZIONE CRISTIAN (prerequisito test)**: `telegram_link` è **vuota** (0 link attivi) → il bot non riconosce nessuno in DM. Cristian deve prima **collegare il suo Telegram** (area soci → `/collega-telegram` → token → `/start <token>` al bot in privato). Solo dopo il suo `telegram_user_id` risolve a super_admin (99) e i comandi rispondono. Poi testare in DM `/cruscotto` `/guardiani` `/notifiche` (+ `on|off`) e in negativo (gruppo → "usa in privata"; non-admin → rifiuto).

### Fasi successive (non ora)
- Fase 2 read-only (DM admin): `/soci` `/pagamenti` `/convenzioni` `/custodi` `/redazione`.
- Fase 3 (scrittura, delicata): approva/rifiuta lemmi/convenzioni da Telegram con conferma.
- DM personale per le notifiche (oggi al gruppo) quando più membri saranno collegati.

---

## ⚠️ Nota tecnica — migration history disallineata (impatta i prossimi `db push`)
`supabase db push --linked` **si blocca**: il DB remoto ha ~38 vecchie migration (`20260421…20260522`) i cui file **non sono nel repo** → il CLI chiede un `migration repair` (che le marcherebbe erroneamente come `reverted`). **NON riparato** (fuori scope, rischioso). Finché la history non viene riconciliata, applicare le nuove migration **via MCP `apply_migration`** (esegue + registra in `schema_migrations`) come fatto qui, oppure dal SQL Editor. Da valutare a mente fredda una riconciliazione pulita (`db pull` in un ramo di prova).

---

## Follow-up / in attesa di Cristian
- **guardiani_contrib**: toggle non ancora wired (serve distinguere contributore nuovo vs esistente sull'upsert per email). Default sarebbe OFF.
- **Digest giornaliero** per tipi rumorosi + **DM personale** a Cristian via `telegram_link` (oggi vuoto): fase 2, brief a sé.
- **Notifiche gruppo "Sala comando"** e comandi admin del bot: pendente storico (serve `chat_id` via `/chatid`).
- Pendenti invariati dal 14/7: CSP enforced (aggiungere host mappa a `img-src` + audit live), Turnstile (site key + secret Cloudflare), `og-termini.jpg`, newsletter broadcast.

---

## Commit della sessione (`origin/main`)
- `e8846f6` Home: immagini sezioni Guardiani de la lenga e Atlante del Ladino
- `5d6e224` Home: richiamo laterale Diventa socio (badge oro pulsante, desktop)
- `72bce28` Notifiche direttivo: sistema config-driven per i buchi (pagamenti, guardiani, alert)
- `08c1a07` download-lead: PII minima nella notifica direttivo (iniziali + risorsa, no email)
- `0faa22a` telegram-bot Fase 1: cruscotto direttivo in DM (/cruscotto /guardiani /notifiche)
