# HANDOFF — El Brenz, migrazione sito pubblico → 6 luglio 2026

## Cos'è questo progetto (e cosa NON è)
Questo repo (`~/Sviluppo/noslab-elbrenz`, GitHub `associazioneelbrenz-ai/noslab-elbrenz`) è il **SITO PUBBLICO** dell'associazione: migrazione da WordPress (`www.elbrenz.eu`) a **Astro 6 + Tailwind**. Vetrina pubblica.

⚠️ **NON è la PWA soci.** La PWA soci (React 19 + Vite + Supabase, con forum/community pubblica+privata) è un **progetto SEPARATO e futuro**: repo diverso, stack diverso (React, non Astro), Supabase/dominio/Netlify propri. Il sito ci si **collegherà** con un pulsante "Community", ma non ne contiene il codice. Non mescolare i due: dare istruzioni React a questo progetto Astro rompe tutto.

## Stato attuale
- Branch di lavoro: **`wip/display-layer`**. Account git da usare: `associazioneelbrenz-ai` (`gh auth switch -u associazioneelbrenz-ai && gh auth setup-git`).
- ✅ **113 articoli** importati dal WXR e classificati (commit `2faa1b0` su `main`): 51 in produzione (Lingua 36, Storia 9, Identità 5, Vita 1), 56 in archivio, 6 bozze. Campo `archivio` nello schema.
- ✅ **Display layer M2.2** (commit `9e9e7c1`, pushato): rotta dettaglio `/articoli/[...slug]`, pagine-pilastro come liste per anno, pagina `/archivio-storico`, helper `src/lib/articoli.ts`, componente `ElencoArticoliPerAnno.astro`.
- ✅ **Nav rifatta** a 4 tendine: L'Associazione · Temi · Progetti · Archivio (Andreas fuori dal menu, doppione Rievocazioni/Eventi eliminato). Committata (`3086dd0`); voci senza pagina = placeholder disabilitati "in arrivo".
- ✅ **Header hero** con foto lago: `hero-lago.jpg` + overlay gradiente verde 65/75/90 (`3d01406`), WebP ottimizzate desktop 1600w/mobile 828w con fallback JPEG via `<picture>` (`99f91d8`).
- ✅ **Home ricca M2.4** (6 luglio, sera): tre blocchi additivi sotto l'intro — "Ultimi articoli" (4 di produzione, helper `lib/articoli.ts`, commit `a2696ba`), collection `eventi` con seed bozza (`ab284ef`), "Prossimi eventi" max 3 futuri con fallback testuale (`cb3ee9a`), card "La nostra comunità" con CTA `/tesseramento` (`3d6e89e`). Decisioni: link "Tutti gli articoli" OMESSO perché non esiste una rotta indice unica (solo dettaglio + pagine-pilastro) — da valutare in futuro una `/articoli/`; nomi campi eventi in italiano (titolo/data/bozza) ≠ convenzione articoli (title/draft), discrepanza voluta dal brief e documentata in `content.config.ts`; `bozza` default false (un evento nasce per essere pubblicato). Validato: build verde, astro check 75 errori = baseline, link home tutti 200.
- 💾 **Foto WP scaricate** via FTP in locale (`~/www.elbrenz.eu/wp-content/gallery/` + `/uploads/`): backup completo delle gallerie Imagely + media library. NON ancora integrate nel sito.
- 📄 **Contenuti pagine WP estratti** (30 pagine, testo in Markdown già pronto — vedi zip `pagine_wp_contenuti`). ~10 con contenuto vero: Fiöi dal Nos (6191 parole), Statuto, Direttivo, Chi siamo, Iniziò così, Vignette, Lunari, Portale Memoria, Contatti.

- ✅ **Pagine istituzionali M2.6** (6-7 luglio, notte): componente `PaginaIstituzionale.astro` + 9 pagine dai contenuti WP estratti (zip `~/Downloads/pagine_wp_contenuti.zip`, scompattato in `docs/pagine_wp_contenuti/` — ESCLUSO dal repo via .gitignore perché i sorgenti contengono recapiti personali). Nav: tutte le voci attive, zero placeholder. **Decisioni prese in autonomia** (delega di Cristian, notte del 6/7):
  - `/chi-siamo` testo WP integrale; immagini hotlinkate WP omesse ovunque (morirebbero al cutover); Google Forms → `/tesseramento`.
  - `/direttivo` SOLO struttura organi da Statuto + "in aggiornamento": la pagina WP era *pending* con bio 2013-14 contraddittorie (per questo era pending). **Nomi attuali da confermare con Cristian.**
  - "Iniziò così....." (pending su WP) NON pubblicata — decisione editoriale da prendere.
  - `/contatti` con dati associativi del footer; recapiti personali WP non riportati (privacy).
  - `/fioi-dal-nos` con LINK YouTube, niente iframe (GDPR: cookie di terze parti senza consenso); la ricerca integrale ~6200 parole andrà pubblicata come articoli Storia/Lingua.
  - `/os-dal-nos` costruita da info verificate (le pagine WP erano shortcode vuoti): 6 edizioni 2014-2019.
  - `/photogallery` in lavorazione (foto scaricate via FileZilla, vedi sopra).
  - Pagine WP minori non migrate per ora: le-vignette-in-italiano (5K, contenuto vero), sostenitori, anno-2010..2014, pagine evento singole, glossary, page-3654 — da valutare.
- 🚀 **Deploy prod** su elbrenz-app.netlify.app eseguito a fine sessione su esplicita delega di Cristian ("vai in produzione con deploy", notte 6/7). DNS/dominio NON toccati.
- ✅ **M2.5 Photogallery + tavole lunari + sostenitori** (7 luglio):
  - **Inventario materiale FileZilla** (`~/www.elbrenz.eu/wp-content/`): `gallery/` COMPLETA, 3 album NextGEN (Beato Carlo 177 jpg, Nossa storia 'n Piaza 61, Presepio 12 = 250 principali + `jpg_backup` originali + thumbs/cache NGG; 214MB). `uploads/` era IN DOWNLOAD durante il lavoro (da 0 a ~1900 file nel corso della sessione) → ricontrollare l'esito a download finito. Le 12 tavole del Lunari 2023 (uploads/2022/11, originali di stampa 10-19MB) recuperate via curl dal sito live.
  - **Curatela /photogallery**: 55 foto su 250 (29+20+6), campionamento cronologico uniforme + revisione visiva su fogli-provino; scartati notturni scuri, doppioni, scatti mossi/tagliati, foto con auto in campo e un primo piano di ragazzo (prudenza minori). Credito fotografo **Silvano Dalla Serra** (watermark) nelle didascalie di Beato Carlo e Presepio; Nossa storia = archivio El Brenz. Statua lignea: scultore Serafino Panizza (dal cartello in foto).
  - **Tecnica**: WebP pre-generate con sharp in scratchpad (pattern hero 99f91d8), grande max 1600px q65-68 + thumb 400px; tavole lunario max 1400px q76 (testo nei pannelli). Manifest dimensioni in `src/lib/gallery-manifest.json`. **WebP senza fallback JPEG** (supporto ~98%, budget peso; l'hero conserva il suo fallback). Lightbox `<dialog>` nativo + JS vanilla inline, zero dipendenze. Peso aggiunto: `public/gallery` 7,2MB + `public/lunari` 2,3MB; `dist` 16→26MB.
  - **Tavole lunari**: sezione "Le tavole dei lunari" in `/lunari-dal-nos` con le 12 tavole 2023 "La nosa storia" + didascalie italiane migrate dalla pagina WP "LE VIGNETTE IN ITALIANO" (dicembre in ladino, corsivo + lang).
  - **Sostenitori**: NON migrata (decisione Cristian), nessuna pagina né voce menu.
  - **INVENTARIO REDIRECT 301** (per il task redirect, punto 5 coda; `public/_redirects` non esiste ancora e l'adapter Netlify emette il proprio — creare con attenzione):
    - `/sostenitori/` → `/tesseramento`
    - `/le-vignette-in-italiano/` → `/lunari-dal-nos`
    - `/photogallery/anno-2010/` … `/photogallery/anno-2014/` → `/photogallery`
    - `/photogallery/anno-2012/commemorazione-del-beato-carlo-dasburgo-9-settembre-male/` → `/photogallery`
    - `/photogallery/anno-2013/la-nossa-storia-n-piaza-21-luglio-male/` → `/photogallery`
    - `/photogallery/anno-2012/presepio-el-brenz-natale-2012-male/` → `/photogallery`
    - `/concorso-os-dal-nos/` e `/progetti/…` (vedi old_url nei pages_md) → pagine progetto corrispondenti
  - Prossimo passo: punto 4 coda = **Resend DKIM/SPF/DMARC**.

- ✅ **M2.R Redesign chiarezza /tesseramento** (7 luglio, eccezione autorizzata): quota deliberata **20€** (aggiornata ovunque: meta, box, copy andreas-chat.js; l'articolo legacy 2015 che cita 10€ nel corpo resta intatto — decisione editoriale), percorso 3 passi, modulo con etichette esplicite, feedback post-invio che ripete il passo 2, sezione aiuto. Logica di invio INVARIATA, testato invio reale (success).
- ✅ **M2.6 Pagamenti PayPal** (7 luglio, eccezione autorizzata per NUOVE edge function): tabella `pagamenti_tesseramento` (RLS senza policy, migration in repo), edge function `paypal-create-order` (quota 20€ HARDCODED server; donazioni 1–500€ validate server; anonime senza dati personali), `paypal-capture-order` (idempotente), `paypal-webhook` (firma obbligatoria, riconciliazione idempotente, mai payer_email). `PAYPAL_ENV` decide live/sandbox. Frontend: `/dona` (NON linkata) + blocco quota su /tesseramento gated `?paypal=1` finché Cristian non completa il test live; flip in `PAYPAL_QUOTA_LIVE`. Smoke test live superati (ordine reale creato e riga ripulita). **Guida operativa: `docs/SETUP_PAYPAL.md`** — mancano: PAYPAL_WEBHOOK_ID (dopo config webhook su developer.paypal.com), test reale quota+donazione+rimborsi, go-live pubblico. Decisione 7/7: sandbox saltata, si testa in live. Nota: client ID pubblico verificato contro SDK (un incolla era corrotto l/I).

- ✅ **Pulizia errori astro check** (7 luglio, richiesta Cristian): `supabase` escluso dal tsconfig (le edge function sono Deno, il TS check di astro produceva ~90 falsi errori) + fix prop `ogImage` sulla pagina Andreas (unico errore reale). **Nuovo baseline: 0 errori, 0 warning** — d'ora in poi qualsiasi errore di `astro check` è un problema vero, niente più "ignora i 75 errori".

- ✅ **M2.6-bis Selettore pagamento + ricevute bonifico con OCR + donazioni** (7 luglio, sera):
  - **Selettore PASSO 2** su /tesseramento: dopo l'invio riuscito il riquadro verde diventa scelta guidata (PayPal/carta · Bonifico · Satispay disattivato "in arrivo"). Bonifico SEMPRE attivo e indipendente dal ramo PayPal; PayPal attivo solo con flag o ?paypal=1.
  - **Upload ricevuta** (facoltativo, jpg/png/pdf max 10MB) su /tesseramento e /dona (àncora #bonifico): edge fn `ricevuta-ocr` salva nel bucket PRIVATO 'ricevute' PRIMA dell'OCR, estrae {importo, valuta, data, ordinante, causale, cro_trn} con Claude Haiku (structured outputs), scrive riga metodo=bonifico stato=**in_verifica**. MAI approvazione/rifiuto automatici: incoerenze → solo flag `anomalia`. Conferma a 'completato' SEMPRE manuale (dashboard Supabase; pannello admin PWA = roadmap). E2E testato: ricevuta finta coerente → tutti i campi estratti, anomalia=false. **Retention: file cancellato dopo conferma o max 12 mesi** (in informativa breve del modulo).
  - **Flag go-live condiviso** `src/lib/pagamenti.ts` (PAGAMENTI_LIVE, build time): governa box "Sostieni El Brenz" in home (non esiste in pagina finché false), riquadro PayPal su /tesseramento, e al go-live va linkata /dona da tesseramento+community.
  - **Box home "Sostieni El Brenz"**: avorio/oro, pill gialla stile PayPal (wordmark testuale, NESSUN widget hosted) → /dona; link secondario → /dona#bonifico. Render solo a flag attivo.
  - **Bugfix feedback modulo**: non riproducibile con browser pulito (probabile rate limit 3/ora/IP o cache SW); hardening: scrollIntoView sul feedback, ramo PayPal in try/catch + sdk.onerror, SW cache v2.
  - **Trappola 11 scoperta e documentata in CLAUDE.md**: commento `//` sopra un import a metà frontmatter .astro rompe l'hoisting del compilatore (errore esbuild con posizione fasulla; astro check passa).
  - **PULIZIA DA FARE dopo i test live di Cristian**: righe di prova in `pagamenti_tesseramento` (quota 20€ test, donazione 1€ anonima, eventuali bonifici di prova) + relativi file nel bucket 'ricevute' + rimborsi PayPal. Le righe/file dei MIEI test tecnici sono già stati ripuliti (7/7 sera).

- ✅ **M2.6-ter Workflow approvazione tessere** (7 luglio, notte): tabella `domande_tesseramento` (19 storiche 1-19 inserite da Cristian, contatore da TESSERA_SEED=20 collaudato), contact-form persiste la domanda + sezione PAGAMENTO live + link firmato alla scheda; webhook aggancia pagamento↔domanda e manda mini-mail "Pagamento ricevuto"/"orfano" (idempotente via `notificato`); edge fn `scheda-domanda` (token HMAC scope-separati, noindex, approva/respingi a un click, log per verbali CD, doppio click innocuo — 5/5 test superati). Tessera = email HTML elegante con logo + bandiera ladina; PDF = fase 2 dichiarata. `ADMIN_ACTION_SECRET` generato e impostato senza transitare in chat; **TESSERE_LIVE SPENTO** (attivare dopo Resend + ok Cristian; MAI invii automatici alle 19 storiche — 2 soci con email condivise da sistemare prima). Solleciti rinnovo NON implementati (regola: mai prima del 31/12; campo `scadenza` pronto).
- ✅ **CUTOVER-READY** (7 luglio, notte): `public/_redirects` con 449 regole 301 (113 articoli con regola esatta data/slug + ?p=ID forzata; pagine WP; archivi generici; 6 bozze→home). Validato: 26/26 cross-link interni, 19/19 campioni su anteprima e ricontrollo in produzione. **Merge `wip/display-layer` → `main` (291ad3f) + deploy prod da main.** DNS = Cristian (mai Claude): sequenza handoff §coda punto 7 + su Netlify aggiungere il custom domain elbrenz.eu/www (dashboard → Domain management) PRIMA di puntare i record.
- ✅ **M2.7 Sezione "Seguici" in home** (7 luglio, notte): card Migole de Storia (3 thumb self-hosted in `public/social/migole/` — **PLACEHOLDER da sostituire con le migole reali di Cristian**, stessi nomi file, quadrate ~400px) + card Facebook con badge inline. Zero SDK/iframe Meta, zero cookie.
- ✅ **M2.8 Rubrica trilingue "Una terra, cinque lingue"** (7 luglio, notte): collection `rubrica` + seed, indice `/una-terra-cinque-lingue` per anno, pagina uscita con switcher ad ancore (ladino SEMPRE primo, sezioni con lang lld-anau/it/de — NON 'lad' come nel brief: è il codice del giudeo-spagnolo), credito+link UT24, voce nav in Progetti. Card home NON aggiunta (home già densa, previsto dal brief). **Backlog uscite = task separato: testi dagli originali di Cristian, NON scraping UT24.**
- 📋 **Accodati post-cutover**: **Meta Pixel** SOLO consent-gated (serve Pixel ID di Cristian + categoria marketing nel cookie banner + aggiornamento cookie policy); import backlog rubrica; migole reali.
- 🧹 **Pulizia test da fare**: righe pagamenti test di Cristian (quota 20€ e donazione 1€ 'completato' + 1 ordine 'creato' abbandonato) dopo rimborsi PayPal — i rimborsi sono anche la prova funzionale della riconciliazione webhook (righe → 'rimborsato').

- ✅ **M2.9 Embed click-to-load** (8 luglio, DEROGA autorizzata da Cristian): la regola "niente cookie di terze parti" evolve in "niente SENZA azione esplicita dell'utente". Componente riutilizzabile `EmbedConsenso.astro`: zero richieste a terzi al page load (verificato con cattura di rete), placeholder con avviso+informativa+fallback, inject solo al click (YouTube su **youtube-nocookie.com**; Instagram embed.js una volta), memoria di sessione via cookie tecnico first-party `eb_embed_*`, un click = tutto il provider in pagina. Banner: categoria "Contenuti esterni"; cookie policy aggiornata (sezione due-click, Meta/Google, data 8/7/2026). `/fioi-dal-nos` ora con video embed. **Migole in home: PRONTE ma spente — servono i 3 URL dei post Instagram da Cristian** (inserirli in `migoleUrls()` in index.astro).

- ✅ **CUTOVER ESEGUITO (8 luglio, mattina, da Cristian)**: `elbrenz.eu` live su Netlify (A 75.2.60.5, CNAME www, SSL ok). **Primary = apex elbrenz.eu** (canonical/og/sitemap già coerenti) — NON cambiare. Smoke test Code: www→apex 301, http→https 301, redirect legacy 4/4, 15 pagine chiave 200, robots+sitemap coerenti con apex. **NUOVA REGOLA: deploy Netlify SOLO Cristian** (Code prepara build e commit e si ferma).
- ⚠️ **ANOMALIA A2 — RISOLTA COME NON-BUG** (indagine 8/7): la domanda "Faldrake" non fu creata perché il modulo fu inviato alle 18:44 UTC del 7/7, PRIMA del deploy di contact-form v18 con la persistenza (19:19:53 UTC, commit 1bf9b83 delle 21:45 CEST) — stessa ragione per domanda_id NULL (webhook v4 anch'esso delle 19:19 UTC, pagamento completato 18:46). Il flusso attuale è corretto. **PROPOSTA FIX (in attesa di decisione Cristian)** per il requisito "quota completata sempre con domanda_id": il webhook, per quota completata senza domanda in attesa corrispondente, CREA una domanda 'in_attesa' dai dati del pagamento (nome/email quando presenti) e la collega — nessuna approvazione automatica; la mail diventa "domanda creata dal pagamento, completare i dati". Alternativa: status quo (mail 'pagamento orfano' al segretario).
- ✅ **STEP 3 chiusura + M2.9-bis** (8 luglio): footer con social button SVG inline (FB/IG/YouTube/YT Music, Telegram commentato TODO), credit NosLab GIÀ esistente (non duplicato), motto corretto in grafia «Raìs fonde no le 'nglacia» ovunque in src + tessera (scheda-domanda v3) — resta in andreas-hofer/index.ts (edge fn Andreas, fuori perimetro, segnalato). **DEROGA ESTESA embed**: EmbedConsenso ora SOLO iframe (mai SDK Meta), migole = 3 reel reali (rapporto 9:16), card Facebook con Page Plugin click-to-load, cookie policy aggiornata. Verifica rete: zero contatti Meta pre-click, reel effettivamente renderizzati post-click. Commit b38e23a/627ad1a su main — **BUILD PRONTA, DEPLOY A CRISTIAN**.
- ⏳ **In attesa di Cristian**: (1) decisione fix A2; (2) rimborsi PayPal dei 2 test → verifica 4/4 (righe → rimborsato via webhook, MAI a mano); (3) OK esplicito go-live → flip PAGAMENTI_LIVE (src/lib/pagamenti.ts, costante frontend build-time) + pulizia 3 righe test per id; (4) verifica dominio Resend su dashboard (record già su Aruba) → TESSERE_LIVE.

- ✅ **M3.0 Widget Andreas overlay** (8 luglio): FabAndreas apre un pannello chat in overlay (desktop 380×560, mobile full, z-40) con AndreasChat riusato via prop `compatto` (pagina /andreas identica); contatore condiviso verificato (3→2 con domanda reale); fascia WhatsApp (Cristian attivo, **Diego predisposto ma spento — attivazione = valorizzare NUMERO_DIEGO in FabAndreas.astro, una riga**); badge '1' con localStorage `andreas_widget_seen` (in cookie policy); **lazy load dello script chat alla prima apertura** → zero jsdelivr al page load su tutte le pagine. Edge fn andreas-chat intoccata. Commit su main — **BUILD PRONTA, DEPLOY A CRISTIAN**.

## Roadmap deliberata (Cristian, 7 luglio 2026) — solo progettazione, NON implementare ora
- **Andreas 2.0 — funzioni di controllo dell'ecosistema**: oltre al RAG pubblico, funzioni di monitoraggio/controllo (stato pagamenti, contenuti, integrazioni). Probabile ponte col backoffice della futura PWA soci. Da progettare.
- **Bot Telegram** dell'associazione: notifiche eventi/articoli ai soci, possibile interfaccia ad Andreas. Da progettare.
- Le email di conferma pagamento si agganciano DOPO il setup Resend (punto 4 coda).

## Coda prossimi passi (ordine)
1. **Chiudere display+menu+header**: verificare/committare, `git push`, poi **fondere `wip/display-layer` su `main`**.
2. ~~**Arricchire la HOME**~~ ✅ FATTO 6 luglio sera (vedi stato sopra). Resta da riempire la collection `eventi` con eventi reali mano a mano (il seed è `bozza: true`).
3. ~~**Pagine istituzionali**~~ ✅ FATTO 6-7 luglio (vedi stato sopra). Mistero *pending* risolto: contenuti Direttivo stantii (2013-14). **Aggiornamento 7 luglio**: `/direttivo` completata con nomi e cariche attuali (fonte: comunicazione diretta di Cristian) — Magnoni presidente, Pangrazzi e Comini vice, Bresadola segretario-tesoriere, 4 consiglieri; solo nome+carica, bio in seguito. "Iniziò così" resta NON pubblicata, confermato da Cristian (si pubblicherà in un secondo momento). Restano: pagine minori WP (vignette, sostenitori), split ricerca Fiöi dal Nos in articoli.
4. **Photogallery** — ricostruire le gallerie dai file scaricati (`gallery/`).
5. **Resend DKIM/SPF/DMARC** (bloccante pre-cutover, email).
6. **301 redirect** — vecchi URL → nuovi, articoli (via `legacy_wp_id`) + pagine (vecchi URL nell'inventario). File `public/_redirects`.
7. **Cutover DNS** su Aruba → sito live. Account Netlify: **`associazione.elbrenz@gmail.com`**. Sequenza: backup → TTL basso → cambio A/CNAME (mai MX/SPF) → verifica → SSL → test.

## Progetto SEPARATO (futuro): PWA soci
React 19 + Vite + Supabase + FCM. Community **pubblica** (vetrina, discussioni in chiaro, "iscriviti") + **privata** (forum soci, archivio, tessere), modello Punto Riflesso. Sessione dedicata, cartella e repo propri, con il documento "Istruzioni PWA" come guida. Il sito Astro linka a questa app; nessun codice condiviso.

## Regole
Regola ferrea: mai rimuovere codice funzionante, solo aggiungere/modificare con ok. `.bak` prima di modifiche. NON toccare: edge function Supabase, Andreas, moduli contatti/tesseramento, cookie/privacy, `/archivio` digitale. Validare con `npm run build` (verde) — ignorare i ~75 errori pre-esistenti delle edge function Supabase (import Deno). Brand: verde `#1E2E26`, oro `#C8923E`, avorio `#F5EEDD`, Playfair. Deploy e dominio li fa Cristian, mai Claude Code.
