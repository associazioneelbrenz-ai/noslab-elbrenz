# HANDOFF — 2026-07-17 (sessione Code)

Account: GitHub `associazioneelbrenz-ai` · Supabase `wacknihvdjxltiqvxtqr`.
Repo toccati: **`noslab-elbrenz`** (sito, HEAD `5dac468`) e **`elbrenz-community`** (app soci, HEAD `7daeeae`, ~28 commit oggi).
Deploy: sito `elbrenz.eu` (netlify site `a8922ddb…`) · app `elbrenz-community.netlify.app` (site `8447c184-c98c-4e00-a198-65502903774b`).

Sessione lunghissima e multi-feature. **Tutto live; l'app è stata collaudata su iPhone reale da Cristian a fine sessione («funziona tutto»).** Sotto, per area, cosa è stato fatto e cosa resta.

---

## 🔓 IL FIX CHIAVE: pubblicazione Community sbloccata
La pubblicazione dei post falliva su iPhone con «TypeError: Load failed» — che **mascherava** il vero errore: il CHECK `forum_thread_tipo_check` ammette solo `'bacheca'|'discussione'`, ma `creaPostFeed` inseriva `tipo:'post'` → Postgres rifiutava OGNI pubblicazione (`forum_thread` era vuota, 0 righe). **Fix (zero migration)**: `creaPostFeed` usa `tipo:'bacheca'`. Verificato via DB. Da qui la Community è diventata testabile e collaudata. (Il fix service worker fatto prima resta come hardening ma non era la causa.)

---

## 🟢 App community — Community stile feed (§5, 3 fasi)
Il forum spartano è ora un **feed social**. Modello: un "post" del feed = `forum_thread` (titolo facoltativo); corpo = il `forum_post` più vecchio; commenti = gli altri post; reazioni = `forum_reazione` (post/thread); media = nuova `forum_media`.
- **Fase 1**: post con media (immagini/PDF), reazioni emoji (👍❤️😄😮😢🙏, attiva verde), commenti FB-style, moderazione admin. Migration `community_feed` (forum_media + RLS deny-by-default, titolo nullable, storage `assets-pubblici/community/{uid}/`). Componenti `community/` (MediaView, ReazioniBar, MediaAllega, PostCard). `lib/forum.ts` esteso (listaFeed, creaPostFeed, creaPostConMedia, caricaMediaCommunity).
- **Fase 2**: video **embed** YouTube/Vimeo (`parseVideoEmbed`, iframe 16:9, niente upload), **paginazione** feed (infinite scroll IntersectionObserver + «Carica altri»), card documento PDF apri/scarica.
- **Fase 3**: **anteprima PDF** (miniatura prima pagina via `pdfjs-dist` importato DINAMICAMENTE, chunk separato ~330kB fuori dal bundle, `globIgnores` nel workbox così non entra in precache; fallback card se fallisce); **menzioni @** (`TestoConMenzioni` + RPC `cerca_soci` security-definer a esposizione minima soci≥10, cattura ids in `forum_post/thread.menzioni` per FCM futuro, NIENTE consegna; `TestoRicco` evidenzia @Nome); **ricerca** dentro la RLS (client, ilike su titolo+contenuto).
- Guardrail rispettati: RPC security-definer per i membri (anon revoked), ricerca senza bypass RLS, menzioni cattura-non-consegna.

## 🟢 App community — restyling + home dinamica
- **Restyling Community** (CSS `cm-*`): card con profondità, avatar anello oro + monogramma, chip canali scroll, badge canale, tempo relativo, reazioni pill, stato vuoto curato, micro-interazioni.
- **Home dinamica**: **logo-moneta** rotante (aquila + logo El Brenz), caroselli **eventi** / **articoli per pilastro** («Dalle nostre valli») / **striscia convenzioni** (marquee, `beneficio_sintetico`), **GitaCard** (countdown 22/8/2026 + posti da `v_posti_gita` + CTA sito), **UltimiCommunity**, sezione **Contribuisci**. **In evidenza** = **vetrina PROGETTI rotante** (auto ~6s + freccia + pallini) da `config_app` chiave `home_evidenza` (categoria `editoriale` = leggibile dal socio). **Card Andreas rimossa** dalla home (autorizzata; resta la FAB col volto). Copertine **404-proof** (componente `Cover` → fallback brandizzato).
- **Immagini «In evidenza»**: 5 card con immagini dedicate WebP/JPG del sito (`img/home/grande-guerra-card.webp`, `mappa-teaser.webp`, `img/home/storie-card.jpg` NUOVA, `img/home/custodi-card.jpg` NUOVA, `img/home/guardiani-de-la-lenga-card.webp`). Storie e Custodi pubblicate oggi (copiate in `public/img/home/` del sito + deploy). Config editabile da `config_app.home_evidenza`.
- **Mappa delle Valli** (`/app/mappa` + card Esplora): **Leaflet self-hosted** (`public/leaflet/`), viste pubbliche `v_luoghi_mappa` (3) + `v_convenzioni_mappa` (2, beneficio breve), marker per categoria + legenda, filtri tipo/valle, tile OSM, popup con escape.

## 🟢 App community — donatore non socio (LAVORO B)
L'ospite (email OTP, livello 1) dona materiale con file, sotto approvazione admin, **distinto** dai soci. Migration `donazione_materiale` (RLS deny-by-default + guardia stato/diritti/rate-limit 5/gg), **bucket privato `donazioni`** (URL firmati per admin), UI `DonaMateriale` su `/non-socio`, curatela `AdminDonazioni` nella dashboard (AAL2), privacy sito punto m). «Caricato ≠ pubblicato».

## 🟢 App community — riconoscimenti Fase 1 (punti/livelli/distintivi)
Motore DB a trigger (SECURITY DEFINER), livelli **cosmetici** (mai gating), niente premi. Migration `riconoscimenti`: `punti_evento` (append-only, socio legge solo i propri, nessun insert socio), `livello` (l0-l5), `distintivo`+`utente_distintivo`, `utente.mostra_livello`, RPC `livello_utente`. Trigger: reazione ricevuta +2 (escl. auto), post +2/commento +1 (tetto 10/gg), milestone museo +20/lemma +25/storia +15, profilo completo +5. UI: `RiconoscimentiProfilo` (barra + distintivi + toggle visibilità), `LivelloChip` nel feed, pagina `/app/riconoscimenti`. Verificato via DB, test data ripuliti.

## 🟢 Andreas nell'app + fix «non interrogabile»
- Popup app → iframe `elbrenz.eu/andreas/embed` (route NUOVA sul sito: chat minimale same-origin senza Layout). FAB col **volto di Andreas**, blocco scroll iOS, bottom-nav resa davvero **fissa** (rimosso `position:relative` che annullava il `fixed`).
- **Andreas era rotto su tutto il sito**: la CSP enforced (16/7) senza `cdn.jsdelivr.net` in script-src bloccava marked/DOMPurify → `ensureLibs()` rifiutava → `init()` abortiva prima di agganciare submit/renderWelcome. Fix: `ensureLibs` in try/catch + jsdelivr in `_headers` + regola `/andreas/embed` che allenta SOLO il framing verso le origin dell'app. Edge `andreas-chat` INTOCCATA.

## 🟢 Sito + varie
- **Tessera 0 istituzionale**: `numero_tessera=0` (info@elbrenz.eu) trattato come valido; nel Profilo mostra l'**emblema aquila del Tirolo + logo El Brenz** al posto del QR. Fix `tessera-invio` (`numero < 0` invece di `< 1`).
- **Storie pubbliche sul sito** già live (`/storie` + `/storie/{id}`, vista `v_storia_pubblica`).
- **Forma ladina «da le»**: corretti residui `public/manifest.webmanifest` + commento `sw.js`.
- **Riga evento gita** creata in `public.evento` (Giochi Medievali dell'Alto Adige, Sluderno/Val Venosta, 22/8/2026, 54 posti, `tipo='gita'`); esclusa dal carosello eventi (`prossimiEventi` filtra `tipo='gita'`, mantenendo i null) per non duplicare la GitaCard.

---

## ⏳ IN CORSO / a fine sessione
- **Iconografia unificata "a sigillo"** (brief 17/7): rimuovere le emoji di sistema (Contribuisci ✍️🖼️📖, fallback ecc.), sostituirle con icone a tratto oro dentro un cerchio-sigillo su avorio; rifinire le card Esplora (copertina+scrim o icon-led). NB: l'app usa un **set icone custom** in `src/lib/icons.tsx` (niente lucide) → estendere QUELLO, non introdurre librerie.

## 🔜 PENDENTI (in mano a Cristian / fasi successive)
- **Testi legali**: privacy del trattamento punti/livelli + media Community/donazioni; Termini d'uso. Prodotti in chat, poi Code li integra in `/privacy` del sito.
- **Nomi definitivi livelli** (eventuale grafia ladina, dalla Commissione — mai inventati).
- **Notifiche push FCM**: disegno pronto, servono le 4 decisioni preliminari. Le menzioni già catturano il dato.
- **Sottodominio `community.elbrenz.eu`**: Netlify + DNS + URL Supabase Auth (azione Cristian).
- **Traduzioni** DE/EN/pt-br/es-cl (dizionari, coi revisori).
- **Fase 3 premi convenzionati**: gate legale DPR 430/2001.
- Rifiniture minori: toast level-up, filtro-categoria mappa, ritagli immagini vetrina.

---

## Note operative
- Un **build+deploy per fase**, un deploy per repo. `config_app.home_evidenza` e le immagini vetrina si aggiornano a **runtime** (nessun deploy app).
- Le migration sono committate in `noslab-elbrenz/supabase/migrations/` e applicate al DB remoto via MCP.
- Collaudo device: dopo un deploy app, **rimuovere+reinstallare la PWA** per svuotare la cache del service worker.

*Handoff 2026-07-17. Sessione monstre: Community feed (3 fasi) + fix pubblicazione, restyling, home dinamica con vetrina progetti/mappa/gita/riconoscimenti, donatore non socio, Andreas riparato, tessera 0. Tutto live e collaudato su iPhone.*
